# SQLite version 3.x
#   gem install sqlite3-ruby (not necessary on OS X Leopard)
development:
  adapter: postgresql
  encoding: utf8
  reconnect: false
  database: busythere
  pool: 5
  host: localhost

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  adapter: mysql2
  encoding: utf8
  reconnect: false
  database: isitbusy_test
  pool: 5
  username: rails_user
  password: L33tfeed
  socket: /tmp/mysql.sock

production:
  adapter: mysql2
  encoding: utf8
  reconnect: false
  database: isitbusy_production
  pool: 5
  username: rails_user
  password: L33tfeed
  socket: /tmp/mysql.sock
